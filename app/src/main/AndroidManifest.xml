<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dipu.testmodule">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".logins">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".QuestionairActivity"
            android:screenOrientation="portrait"
            android:label="Questionnaire">

        </activity>

        <activity android:name=".ReportActivity"
            android:screenOrientation="portrait"
            android:label="Reports">

        </activity>

        <activity android:name=".ModuleList"></activity>
        <activity android:name=".EmployeeDetailsActivity"></activity>
        <activity android:name=".ManagerActivity"> </activity>

    </application>

</manifest>